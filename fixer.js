/* The new version of mBank transaction panel is using a lifting class in html and body tag. */
/* So we just remove them. */

document.documentElement.classList.remove('lifting');
document.body.classList.remove('lifting');